Como heroku funciona con git, no puedo subir la informacion del proyecto a github, por tanto, para volver a ejecutar:
En el directorio de heroku:
git init
git add .
git commit -m 'message'
git push heroku master

